const initialState = {
    isSigned: false,
    isLogin: false

}

const Reducer = (state = initialState, action) => {
    // switch (action.type) {
    //     case "Login":
    //         return state

    //     default: return state
    // }


    return state
}

export default Reducer